<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- SYSTEM_ALERT_WINDOW: the whole point — draw an overlay on top of everything.
         On rooted/priv-app installs this is auto-granted; otherwise user taps
         "Allow" in the activity. -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- FOREGROUND_SERVICE: Android 9+ requires a foreground service for
         long-running overlays. specialUse lets us avoid the more restrictive
         dataSync / mediaProjection categories. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!-- Post notifications (Android 13+) — the foreground service notification needs
         this to be visible. Not strictly required; service still runs if denied. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Network: talk to localhost:8080 (peko-agent). Uses cleartext HTTP to
         loopback, which networkSecurityConfig whitelists. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- BOOT_COMPLETED: bring the overlay up on every boot without the
         user having to open the launcher. This is the "agent as OS"
         contract — peko should be ambient, not a manually-relaunched app. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Phase 5 audio bridge — see AudioBridgeService.kt.
         RECORD_AUDIO is dangerous; granted at boot via service.sh.
         FOREGROUND_SERVICE_MICROPHONE / _MEDIA_PLAYBACK lets AudioRecord
         and AudioTrack stay live while the user is in another app. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!-- Phase 23 vendor-binder shim. Each row is the Android-permission
         hook that gets us to the underlying HAL through the framework
         (camera HAL via CameraDevice, gnss HAL via LocationManager,
         radio HAL via TelephonyManager). pm grant flips them at boot. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA" />

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />

    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_NUMBERS" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".PekoOverlayApp"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@android:style/Theme.Translucent.NoTitleBar"
        android:allowBackup="false"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:excludeFromRecents="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".OverlayService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="draggable agent overlay that must stay resident while user navigates other apps" />
        </service>

        <!-- Phase 5 — peko-agent ↔ AudioRecord / AudioTrack / TTS bridge.
             FGS type 'microphone|mediaPlayback' so recording + playback
             both work when the screen is off / user is in another app. -->
        <service
            android:name=".AudioBridgeService"
            android:exported="false"
            android:foregroundServiceType="microphone|mediaPlayback" />

        <!-- Phase 23 vendor-binder shim services. -->
        <service
            android:name=".LocationBridgeService"
            android:exported="false"
            android:foregroundServiceType="location" />

        <service
            android:name=".CameraBridgeService"
            android:exported="false"
            android:foregroundServiceType="camera" />

        <service
            android:name=".TelephonyBridgeService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="read-only telephony info bridge for the agent" />
        </service>

        <!-- Auto-start the overlay on every boot. See BootReceiver.kt for
             the full rationale; TL;DR: "agent as OS" means peko should
             be ambient, not something the user has to relaunch. -->
        <receiver
            android:name=".BootReceiver"
            android:exported="true"
            android:directBootAware="false">
            <intent-filter android:priority="999">
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
